spring:
  cloud:
    config:
      enabled: false   # 🔥 disables config server

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop

eureka:
  client:
    enabled: false     # 🔥 disables eureka

jwt:
  secret: test-secret-key-which-is-long-enough-1234567890