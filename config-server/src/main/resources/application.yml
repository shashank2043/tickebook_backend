spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/shashank2043/tickebook_config
          username: shashank2043
          password: ${GIT_TOKEN}
server:
  port: 8888
